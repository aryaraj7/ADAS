# ADAS — Phase 1 Dependencies
# Install with:  pip install -r requirements.txt

# ── Core ─────────────────────────────────────────────────────
# YOLOv8 + PyTorch integration (ultralytics bundles torch dep specs)
ultralytics>=8.2.0

# Computer vision
opencv-python>=4.9.0

# Numerical computing (ultralytics pulls this in, listed for clarity)
numpy>=1.24.0

# ── MiDaS Depth Estimation ───────────────────────────────────
# PyTorch (required for MiDaS via torch.hub)
torch>=1.8.0
torchvision>=0.9.0

# timm — required by MiDaS DPT models (DPT_Large / DPT_Hybrid)
timm>=0.6.0

# ── Hand Detection ─────────────────────────────────────────────
mediapipe>=0.10.0

# ── GUI ────────────────────────────────────────────────────────
customtkinter>=5.2.0
Pillow>=10.0.0

# ── Labeling (for custom object training) ──────────────────────
# labelImg                # uncomment to install labeling tool
PyYAML>=6.0.0

# ── Optional (uncomment if you add audio alerts later) ───────
# playsound>=1.3.0
# pygame>=2.5.0
